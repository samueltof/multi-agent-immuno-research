{
  "dependencies": ["."],
  "graphs": {
    "main_workflow": "./src/workflow.py:graph"
  },
  "env": ".env",
  "python_version": "3.12",
  "dockerfile_lines": [
    "RUN apt-get update && apt-get install -y build-essential",
    "RUN pip install --no-cache-dir uv"
  ]
} 
